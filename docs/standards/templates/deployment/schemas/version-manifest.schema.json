{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "published version manifest",
  "type": "object",
  "required": ["appName", "appVersion", "components"],
  "additionalProperties": true,
  "properties": {
    "appName": { "type": "string" },
    "appVersion": { "type": "string" },
    "deployToolVersion": { "type": "string" },
    "gitSha": { "type": "string" },
    "builtAt": { "type": "string" },
    "app": { "$ref": "#/$defs/hooks" },
    "components": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "object",
        "required": ["type", "version", "package"],
        "allOf": [{ "$ref": "#/$defs/hooks" }],
        "properties": {
          "type": {
            "enum": ["infra", "dbschema", "api", "library", "ui", "glue"]
          },
          "version": { "type": "string" },
          "package": { "type": "string" },
          "changed": { "type": "boolean" },
          "public": { "type": "boolean" },
          "dependsOn": { "type": "array", "items": { "type": "string" } }
        }
      }
    }
  },
  "$defs": {
    "hookArray": { "type": "array", "items": { "type": "string" } },
    "hooks": {
      "type": "object",
      "properties": {
        "preDeploy": { "$ref": "#/$defs/hookArray" },
        "deploy": { "$ref": "#/$defs/hookArray" },
        "verify": { "$ref": "#/$defs/hookArray" },
        "postDeploy": { "$ref": "#/$defs/hookArray" }
      }
    }
  }
}
