{
  "//": "Canonical source: the toolkit's src/versioning/releaseConfig.ts (ADR-0001). This file is the LITERAL OUTPUT of the generator; adopting the toolkit swaps it for generated config (see template README). Replace <APP_NAME> with your app code.",
  "tagFormat": "<APP_NAME>-api@${version}",
  "branches": [
    "main",
    {
      "name": "feature/*",
      "prerelease": "${name.replace(/[^a-zA-Z0-9-]/g, '-')}"
    }
  ],
  "plugins": [
    ["@semantic-release/commit-analyzer", { "releaseRules": [] }],
    "@semantic-release/release-notes-generator",
    ["@semantic-release/github", { "assets": [] }]
  ]
}
