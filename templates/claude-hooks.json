{
  "//": "Project-scoped agent hooks for Claude Code — merge into the consuming repository's .claude/settings.json. Enforces at the point of modification: the formatter runs on every file the agent edits, so gate refusals at commit time become rare. Other harnesses: wire the same two behaviours with that harness's hook mechanism.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "npx --no-install prettier --write \"$CLAUDE_FILE_PATHS\" 2>nul || true"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npm run --silent verify || echo 'verify failed — the task is not complete until it passes'"
          }
        ]
      }
    ]
  }
}
